# Fix this

## pre-commit

```bash
# To get a sample .pre-commit-config.yaml
pre-commit sample-config
# To set up the git hook scripts run
pre-commit install
# when adding new hooks:
# WARNING - not sure if there is a --check flag, may change files.
pre-commit run --all-files
```
